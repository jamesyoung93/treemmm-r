# Generate the synthetic pharma DGP (NegBin)

Generate the synthetic pharma DGP (NegBin)

## Usage

``` r
generate_pharma_dataset(
  n_customers = 500L,
  n_periods = 24L,
  random_state = 42L,
  with_adstock = FALSE
)
```

## Value

A `generated_dataset` list.
