# Fit the PyMC-Hier-Oracle baseline via brms (+ planted interactions)

Fit the PyMC-Hier-Oracle baseline via brms (+ planted interactions)

## Usage

``` r
fit_bayesian_hier_oracle(
  df,
  config,
  planted_interactions,
  n_chains = 2L,
  n_iter = 1000L
)
```
