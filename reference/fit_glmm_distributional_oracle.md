# Fit the GLMMDist-Oracle baseline (distributional GLM + planted interactions)

Fit the GLMMDist-Oracle baseline (distributional GLM + planted
interactions)

## Usage

``` r
fit_glmm_distributional_oracle(
  df,
  config,
  planted_interactions,
  family_override = NULL
)
```
