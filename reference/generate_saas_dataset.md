# Generate the synthetic SaaS DGP (ZI-Gamma)

Generate the synthetic SaaS DGP (ZI-Gamma)

## Usage

``` r
generate_saas_dataset(n_customers = 500L, n_periods = 24L, random_state = 42L)
```

## Value

A `generated_dataset` list.
