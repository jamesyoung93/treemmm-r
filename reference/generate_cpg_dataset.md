# Generate the synthetic CPG DGP (Tweedie)

Generate the synthetic CPG DGP (Tweedie)

## Usage

``` r
generate_cpg_dataset(
  n_customers = 200L,
  n_periods = 36L,
  random_state = 42L,
  with_adstock = FALSE
)
```

## Value

A `generated_dataset` list.
